--// kencute24 | LOW ALL FIX LAG + FASTFLAG (CODE ONLY)

-- FASTFLAG (nếu executor hỗ trợ)
pcall(function()
    if setfflag then
        setfflag("DebugDisableFPSLock", "True")
        setfflag("HumanoidParallelRemoveNoPhysics", "True")
        setfflag("HumanoidParallelRemoveNoPhysicsNoSim", "True")
        setfflag("TaskSchedulerTargetFps", "240")
        setfflag("RenderDistanceScale", "0.1")
        setfflag("GraphicsQualityLevel", "1")
    end
end)

-- FPS CAP
pcall(function()
    if setfpscap then
        setfpscap(60)
    end
end)

-- SERVICES
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")

-- LIGHTING LOW
for _,v in ipairs(Lighting:GetChildren()) do
    if v:IsA("PostEffect") then
        v.Enabled = false
    end
end
Lighting.GlobalShadows = false
Lighting.EnvironmentDiffuseScale = 0
Lighting.EnvironmentSpecularScale = 0
Lighting.FogEnd = 1e9
Lighting.Brightness = 1

-- LOW PART
local function LowPart(o)
    if o:IsA("BasePart") then
        o.Material = Enum.Material.Plastic
        o.Reflectance = 0
        o.CastShadow = false
    end
end

-- REMOVE TEXTURE
local function RemoveTex(o)
    if o:IsA("Texture") or o:IsA("Decal") then
        o:Destroy()
    end
end

-- REMOVE EFFECT
local function RemoveFx(o)
    if o:IsA("ParticleEmitter")
    or o:IsA("Trail")
    or o:IsA("Beam")
    or o:IsA("Fire")
    or o:IsA("Smoke")
    or o:IsA("Sparkles") then
        o.Enabled = false
    end
end

-- APPLY ALL
for _,v in ipairs(Workspace:GetDescendants()) do
    LowPart(v)
    RemoveTex(v)
    RemoveFx(v)
end

Workspace.DescendantAdded:Connect(function(v)
    task.wait()
    pcall(function()
        LowPart(v)
        RemoveTex(v)
        RemoveFx(v)
    end)
end)

-- UI
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()
local Window = Rayfield:CreateWindow({
    Name = "kencute24 | LOW ALL FIX LAG",
    LoadingTitle = "kencute24",
    LoadingSubtitle = "Low All + FastFlag",
    ConfigurationSaving = {Enabled = false},
    KeySystem = false
})

local Main = Window:CreateTab("LOW ALL", 4483362458)
local Info = Window:CreateTab("INFO", 4483362458)

Main:CreateButton({
    Name = "RE-APPLY LOW ALL",
    Callback = function()
        for _,v in ipairs(Workspace:GetDescendants()) do
            LowPart(v)
            RemoveTex(v)
            RemoveFx(v)
        end
    end
})

Info:CreateParagraph({
    Title = "kencute24",
    Content =
        "LOW ALL FIX LAG\n"..
        "Texture removed\n"..
        "Effects disabled\n"..
        "FastFlag if supported\n\n"..
        "Discord:\nhttps://discord.gg/A98NkS4Xn\n"..
        "TikTok:\nhttps://tiktok.com/@kencute24"
})

Rayfield:Notify({
    Title = "kencute24",
    Content = "LOW ALL APPLIED",
    Duration = 4
})
